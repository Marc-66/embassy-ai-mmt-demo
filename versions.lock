# Pinned dependency versions — DO NOT edit manually.
# Bump versions via:  make update-toolkit / make update-industry
#                     .\update-toolkit.ps1  / .\update-referencemodels.ps1
#
# All CI workflows read from this file. "make validate" is deterministic.
# To get new versions, run "make update-all" which opens a PR; never fetch latest in CI.

# kairos-ontology-toolkit release tag (without the leading "v")
TOOLKIT_VERSION=1.1.3

# Pinned commit SHA of the kairos-ontology-referencemodels submodule.
# Updated by update-referencemodels.ps1 / make update-industry.
# Never use "main" or "HEAD" here — always a full 40-char SHA.
REFERENCE_MODELS_REF=8166c952d6701c5efefa576bdcfecee0a5c41b5e
